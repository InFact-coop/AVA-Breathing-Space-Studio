import { GrSupport } from 'react-icons/gr'

export default {
  name: 'supportFilterCategory',
  title: 'Support Filter Category',
  type: 'document',
  icon: GrSupport,
  fields: [
    {
      name: 'title',
      title: 'Title',
      type: 'string',
    },
  ],
}
